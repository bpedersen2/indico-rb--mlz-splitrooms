# indico-rb--mlz-splitrooms

indico plugin to handle splittable rooms in roombooking


Configuration: In the plugin settings, provide a json array mapping parent room ids to child room ids

example : "{"4:[5,6]}"
